apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ metadata.name }}
  namespace: {{ metadata.namespace }}
status:
{%- if status.readyReplicas %}
  readyReplicas: {{ status.readyReplicas }}
{%- endif %}
---
