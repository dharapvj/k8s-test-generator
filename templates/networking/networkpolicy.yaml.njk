apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ metadata.name }}
  namespace: {{ metadata.namespace }}
spec:
  policyTypes:
{%- for pol in spec.policyTypes %}
  - {{ pol }}
{%- endfor %}
---
